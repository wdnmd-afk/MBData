# Metabase 数据存储位置详解

> 理解 Metabase 的数据存储方式和位置

---

## 📋 目录

1. [数据存储概述](#数据存储概述)
2. [H2 数据库文件](#h2-数据库文件)
3. [PostgreSQL 数据存储](#postgresql-数据存储)
4. [数据迁移](#数据迁移)
5. [备份与恢复](#备份与恢复)

---

## 🎯 数据存储概述

### Metabase 存储的数据类型

Metabase 需要存储两类数据:

#### 1. Metabase 元数据(配置数据)

**存储内容**:
- ✅ 用户账号和权限
- ✅ 仪表板配置
- ✅ 问题(查询)定义
- ✅ 数据库连接信息
- ✅ 邮件设置
- ✅ 查询历史
- ✅ 缓存数据

**存储位置**: 
- H2 数据库(默认)
- PostgreSQL(推荐,我们使用的)
- MySQL(可选)

#### 2. 业务数据(你要分析的数据)

**存储内容**:
- ❌ **不存储在 Metabase 中**
- ✅ 存储在你自己的业务数据库中
- ✅ Metabase 只是连接和查询

---

## 📁 H2 数据库文件

### 你发现的文件

```
e:\github\metabase\
├── metabase.db.mv.db       (712 KB)  ← H2 数据库主文件
└── metabase.db.trace.db    (51 KB)   ← H2 数据库日志文件
```

### 这是什么?

**H2 数据库** 是 Metabase 的默认内嵌数据库:

| 特性 | 说明 |
|------|------|
| **类型** | 内嵌式文件数据库 |
| **文件** | `.mv.db` 和 `.trace.db` |
| **优点** | 简单,无需配置 |
| **缺点** | 不适合生产环境 |
| **用途** | 测试和开发 |

### 文件说明

#### metabase.db.mv.db

- **主数据文件**
- 存储所有 Metabase 配置数据
- 大小: 712 KB
- 创建时间: 2025-11-20 11:27

#### metabase.db.trace.db

- **日志文件**
- 记录数据库操作日志
- 大小: 51 KB
- 用于调试和错误追踪

### 为什么会有这些文件?

**可能的原因**:

1. **之前运行过简单模式**
   ```bash
   # 直接运行 Metabase JAR 文件
   java -jar metabase.jar
   ```

2. **使用了 docker-compose-simple.yml**
   ```bash
   docker-compose -f docker-compose-simple.yml up
   ```

3. **测试运行**
   - 没有配置外部数据库
   - Metabase 自动创建 H2 数据库

---

## 🐘 PostgreSQL 数据存储

### 当前部署方式

你现在使用的是 **PostgreSQL 作为元数据库**:

```yaml
# docker-compose.yml
services:
  metabase:
    environment:
      MB_DB_TYPE: postgres          # 使用 PostgreSQL
      MB_DB_HOST: postgres          # 连接到 postgres 容器
      MB_DB_DBNAME: metabase        # 数据库名称
      
  postgres:
    image: postgres:15-alpine
    volumes:
      - postgres-data:/var/lib/postgresql/data  # 数据存储位置
```

### 数据存储位置

#### Docker 卷(Volume)

```
Docker Volume: kd_metabase_postgres-data
├── 物理位置: Docker 管理的存储区域
├── 容器内路径: /var/lib/postgresql/data
└── 内容: PostgreSQL 数据库文件
```

**查看卷信息**:
```bash
# 列出所有卷
docker volume ls

# 查看卷详情
docker volume inspect kd_metabase_postgres-data
```

**输出示例**:
```json
[
  {
    "Name": "kd_metabase_postgres-data",
    "Driver": "local",
    "Mountpoint": "/var/lib/docker/volumes/kd_metabase_postgres-data/_data",
    "Labels": {
      "com.docker.compose.project": "kd_metabase",
      "com.docker.compose.version": "2.40.3"
    }
  }
]
```

---

## 📊 数据存储对比

### H2 vs PostgreSQL

| 特性 | H2 数据库 | PostgreSQL |
|------|-----------|------------|
| **文件位置** | `metabase.db.mv.db` | Docker Volume |
| **可见性** | ✅ 可以直接看到文件 | ❌ 在 Docker 内部 |
| **备份** | ⚠️ 复制文件 | ✅ 使用 pg_dump |
| **性能** | ⚠️ 较差 | ✅ 优秀 |
| **生产环境** | ❌ 不推荐 | ✅ 推荐 |
| **高可用** | ❌ 不支持 | ✅ 支持 |
| **数据迁移** | ⚠️ 困难 | ✅ 容易 |

---

## 🔄 数据存储架构

### 完整架构图

```
┌─────────────────────────────────────────────────────────┐
│                   Metabase 应用                         │
│              (metabase-app 容器)                        │
│                                                         │
│  - Web 界面 (http://localhost:3000)                    │
│  - 查询引擎                                             │
│  - 可视化工具                                           │
└────────────┬────────────────────────────────────────────┘
             │
             │ 连接 1: 存储自己的配置数据
             ↓
┌─────────────────────────────────────────────────────────┐
│            Metabase 元数据库                            │
│         (metabase-postgres 容器)                        │
│                                                         │
│  PostgreSQL 15-alpine                                   │
│  数据存储: Docker Volume                                │
│  卷名: kd_metabase_postgres-data                        │
│                                                         │
│  存储内容:                                              │
│  ├── 用户账号和权限                                     │
│  ├── 仪表板配置                                         │
│  ├── 查询定义                                           │
│  ├── 数据库连接信息                                     │
│  └── 查询历史                                           │
└─────────────────────────────────────────────────────────┘
             │
             │ 连接 2: 查询业务数据(需要在 Web 界面添加)
             ↓
┌─────────────────────────────────────────────────────────┐
│              你的业务数据库                             │
│          (在你自己的服务器上)                           │
│                                                         │
│  - MySQL / PostgreSQL / MongoDB / ...                   │
│  - 存储你要分析的实际业务数据                           │
│  - Metabase 不会修改这些数据                            │
│  - 只是连接和查询                                       │
└─────────────────────────────────────────────────────────┘
```

---

## 🗂️ 数据文件位置总结

### 1. H2 数据库(旧的,不再使用)

```
位置: e:\github\metabase\metabase.db.mv.db
用途: 之前测试时创建的
状态: 不再使用(现在用 PostgreSQL)
```

### 2. PostgreSQL 数据(当前使用)

```
位置: Docker Volume (kd_metabase_postgres-data)
物理路径: Docker 管理的存储区域
容器内路径: /var/lib/postgresql/data
用途: 存储 Metabase 所有配置数据
```

### 3. 业务数据(你要分析的数据)

```
位置: 你自己的数据库服务器
说明: Metabase 不存储业务数据,只是连接查询
```

---

## 🔍 查看数据存储

### 查看 Docker 卷

```bash
# 列出所有卷
docker volume ls

# 查看 PostgreSQL 数据卷详情
docker volume inspect kd_metabase_postgres-data
```

### 查看 PostgreSQL 数据库

```bash
# 进入 PostgreSQL 容器
docker exec -it metabase-postgres psql -U metabase -d metabase

# 查看所有表
\dt

# 查看用户表
SELECT * FROM core_user;

# 退出
\q
```

### 查看数据库大小

```bash
# 进入容器
docker exec -it metabase-postgres sh

# 查看数据目录大小
du -sh /var/lib/postgresql/data

# 退出
exit
```

---

## 💾 备份与恢复

### 备份 PostgreSQL 数据

#### 方法 1: 使用 pg_dump(推荐)

```bash
# 备份数据库
docker exec metabase-postgres pg_dump -U metabase metabase > metabase_backup_$(date +%Y%m%d).sql

# 压缩备份
gzip metabase_backup_$(date +%Y%m%d).sql
```

#### 方法 2: 备份 Docker 卷

```bash
# 停止服务
docker-compose down

# 备份卷
docker run --rm \
  -v kd_metabase_postgres-data:/data \
  -v $(pwd):/backup \
  alpine tar czf /backup/postgres-data-backup.tar.gz -C /data .

# 重启服务
docker-compose up -d
```

### 恢复 PostgreSQL 数据

#### 从 SQL 文件恢复

```bash
# 停止 Metabase(保持 PostgreSQL 运行)
docker stop metabase-app

# 恢复数据
cat metabase_backup_20251120.sql | docker exec -i metabase-postgres psql -U metabase -d metabase

# 重启 Metabase
docker start metabase-app
```

#### 从卷备份恢复

```bash
# 停止服务
docker-compose down

# 删除旧卷
docker volume rm kd_metabase_postgres-data

# 创建新卷
docker volume create kd_metabase_postgres-data

# 恢复数据
docker run --rm \
  -v kd_metabase_postgres-data:/data \
  -v $(pwd):/backup \
  alpine tar xzf /backup/postgres-data-backup.tar.gz -C /data

# 重启服务
docker-compose up -d
```

---

## 🔄 数据迁移

### 从 H2 迁移到 PostgreSQL

如果你想保留 H2 数据库中的数据:

#### 1. 使用 Metabase 迁移工具

```bash
# 下载 Metabase JAR
wget https://downloads.metabase.com/latest/metabase.jar

# 运行迁移
java -jar metabase.jar load-from-h2 metabase.db
```

#### 2. 手动迁移

1. 导出 H2 数据
2. 转换为 PostgreSQL 格式
3. 导入到 PostgreSQL

**注意**: 这个过程比较复杂,建议重新配置。

---

## 📝 数据清理

### 清理 H2 数据库文件

如果确定不再需要 H2 数据:

```bash
# 删除 H2 数据库文件
rm e:/github/metabase/metabase.db.mv.db
rm e:/github/metabase/metabase.db.trace.db
```

### 清理 PostgreSQL 数据

```bash
# 停止服务
docker-compose down

# 删除数据卷
docker volume rm kd_metabase_postgres-data

# 重新部署(会创建新的空数据库)
docker-compose up -d
```

---

## 🎯 最佳实践

### 1. 使用 PostgreSQL

- ✅ 生产环境必须使用 PostgreSQL 或 MySQL
- ❌ 不要在生产环境使用 H2

### 2. 定期备份

```bash
# 每天自动备份
0 2 * * * docker exec metabase-postgres pg_dump -U metabase metabase | gzip > /backup/metabase_$(date +\%Y\%m\%d).sql.gz
```

### 3. 监控存储空间

```bash
# 查看卷大小
docker system df -v
```

### 4. 数据安全

- ✅ 定期备份
- ✅ 使用强密码
- ✅ 限制数据库访问
- ✅ 加密敏感数据

---

## 📊 存储空间估算

### Metabase 元数据库大小

| 使用情况 | 预估大小 |
|---------|---------|
| 小型团队(10人) | 100-500 MB |
| 中型团队(50人) | 500 MB - 2 GB |
| 大型团队(200人) | 2-10 GB |

**影响因素**:
- 用户数量
- 仪表板数量
- 查询历史
- 缓存数据

---

## 🆘 常见问题

### Q1: H2 文件可以删除吗?

**A**: 如果你现在使用 PostgreSQL,并且不需要 H2 中的数据,可以删除。

```bash
rm metabase.db.mv.db metabase.db.trace.db
```

### Q2: PostgreSQL 数据在哪里?

**A**: 在 Docker Volume 中,由 Docker 管理:

```bash
docker volume inspect kd_metabase_postgres-data
```

### Q3: 如何查看 PostgreSQL 数据?

**A**: 进入容器使用 psql:

```bash
docker exec -it metabase-postgres psql -U metabase -d metabase
```

### Q4: 数据会丢失吗?

**A**: 只要不删除 Docker Volume,数据就不会丢失:

- ✅ 重启容器: 数据保留
- ✅ 重新部署: 数据保留
- ❌ 删除卷: 数据丢失

### Q5: 如何备份数据?

**A**: 使用 pg_dump:

```bash
docker exec metabase-postgres pg_dump -U metabase metabase > backup.sql
```

---

## 📚 相关文档

- [PostgreSQL 官方文档](https://www.postgresql.org/docs/)
- [Docker Volume 文档](https://docs.docker.com/storage/volumes/)
- [Metabase 数据库配置](https://www.metabase.com/docs/latest/installation-and-operation/configuring-application-database)

---

## ✅ 总结

### 关键要点

1. **H2 文件** (`metabase.db.*.db`)
   - 旧的测试数据
   - 不再使用
   - 可以删除

2. **PostgreSQL 数据**
   - 当前使用的数据库
   - 存储在 Docker Volume
   - 需要定期备份

3. **业务数据**
   - 不存储在 Metabase
   - 在你自己的数据库中
   - Metabase 只是连接查询

4. **数据安全**
   - 定期备份 PostgreSQL
   - 监控存储空间
   - 使用强密码

---

**现在你完全了解 Metabase 的数据存储了!** 📊✨
